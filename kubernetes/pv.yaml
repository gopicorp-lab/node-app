apiVersion: v1
kind: PersistentVolume

metadata:
  name: node-app-pv

spec:
  capacity:
    storage: 1Gi

  accessModes:
    - ReadWriteOnce

  persistentVolumeReclaimPolicy: Retain

  hostPath:
    path: /data/node-app